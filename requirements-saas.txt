# ─── ShortForge SaaS — dependencies (no GPU packages) ────────────────────────

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Database ---
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# --- Task queue ---
celery[redis]>=5.4.0
redis>=5.0.0

# --- Storage ---
boto3>=1.35.0

# --- Configuration ---
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# --- Security ---
cryptography>=43.0.0
passlib[bcrypt]>=1.7.4
pydantic[email]>=2.0.0

# --- Billing ---
stripe>=8.0.0

# --- Scheduling ---
croniter>=2.0.0

# --- External API clients ---
anthropic>=0.40.0
httpx>=0.27.0
requests>=2.31.0

# --- Pipeline dependencies (CPU-only, reused from existing code) ---
edge-tts>=6.1.0
soundfile
numpy>=1.26.0
Pillow>=10.0.0
pysubs2>=1.6.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
